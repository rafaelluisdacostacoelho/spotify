const { assert } = require('chai');
const { playlistsService } = require('../../services/playlists.service');

module.exports = () => {
    it('should return a single playlist', () => {
        assert.equal(1, 1);
    });

    it('should list all playlists', () => {
        assert.equal(1, 1);
    });

    it('should create an playlist', () => {
        assert.equal(1, 1);
    });

    it('should updates an playlist', () => {
        assert.equal(1, 1);
    });
};